---
import { getCollection } from 'astro:content';

import type { Post } from '../types';
// 获取所有文章，并按日期倒序排列
const posts = await getCollection('posts');
console.log(posts);
posts.sort((a: Post, b: Post) => b.data.date.valueOf() - a.data.date.valueOf());

---
