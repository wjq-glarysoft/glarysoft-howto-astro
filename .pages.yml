media:
  input: public/img
  output: /img
content:
  - name: posts
    label: Posts
    type: collection
    path: src/content/posts
    view:
      fields: [ title, date, categories ]
    fields:
      - { name: title, label: Title, type: string, required: true }
      - { name: date, label: pubDatetime, type: date }
      - { name: slug, label: Slug, type: string, pattern: { regex: '^[a-z0-9-]+$', message: '只能包含小写字母、数字和连字符' }  }
      - { name: categories, label: Categories, type: string, list: true }      
      - { name: author, label: Author, type: string }
      - { name: body, label: Body, type: rich-text, options: { input: src/assets/images, output: /assets/images}}
          
  - name: about
    label: About page
    type: file
    path: src/pages/about.md
    fields:
      - { name: layout, type: string, hidden: true, default: "../layouts/AboutLayout.astro" }
      - { name: title, label: Title, type: string }
      - { name: body, label: Body, type: rich-text, options: { input: public/assets, output: /assets } }